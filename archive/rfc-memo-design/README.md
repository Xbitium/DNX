# Archived: RFC-memo styled site (pre-rebrand)

These are the original DNX site pages — RFC-memo aesthetic, terminal replay
of the first cross-NAT ping, side-by-side TCP/IP vs DNX packet anatomy
diagram, and the registry's "birth certificate" log — superseded when
dnxroute.com was rebuilt around the "Route identities, not locations"
positioning (see the live site/ directory instead).

Kept here for reference. Not published — outside netlify.toml's publish path.
